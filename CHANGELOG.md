# Changelog

All notable changes to this project are logged here, newest first. Timestamps are IST.

**Standing workflow**: before starting any new task, read this file and `MEMORY.md` first. After finishing a task, append an entry here (with an IST timestamp) and update `MEMORY.md`'s relevant sections + "Last Updated" date. When a change is made to `mobile/`, the equivalent responsive behavior/UI must also be checked/applied on `frontend/` (and vice versa) in the same pass, per the web↔mobile parity rule — call out explicitly if a change is intentionally single-surface.

---

## 2026-09-19 — Announcements redesign (Flutter + web synced) + splash/login logo fix
- **Flutter**: `announcements_screen.dart` and Home `_buildRecentAnnouncementCard` — removed NEW badge and all green/gradient; flat themed surface card with brand-blue icon chip and link, follows light/dark automatically. `flutter analyze` clean.
- **Web (parity)**: `Summary.jsx` Recent Announcements card and `EmployeeAnnouncements.jsx` list rebuilt identically with `brand`/`surface`/`ink` tokens; responsive (`md:` padding/type on the list). ESLint 0 errors.
- **Flutter only**: `splash_screen.dart` / `login_screen.dart` logo tile pinned to white in dark mode — web has no dark mode, so nothing to mirror.

## 2026-09-19 06:51 IST — Web: employee page redesigns (Profile, Leaves, Notifications) + Salary Preview button
- **Salary cards/table now show the payslip month (e.g. "September 2026") instead of a date** (`View.jsx`; desktop column renamed "Payslip Month"). Flutter `payslips_screen.dart` already showed `monthName year` on the card and preview sheet — parity confirmed, no Flutter change. A transient `FiEye is not defined` console error was a stale HMR snapshot between two edits; the import is in place and the build is clean.
- **Salary (`components/salary/View.jsx`)**: added a **Preview** button (opens the existing payslip detail modal) next to **Download** on each mobile card — matching the Flutter `payslips_screen.dart` Preview/Download pair — and a preview (eye) icon beside the download icon in the desktop table. Flutter already had both; no Flutter change needed.
- **Profile (`components/EmployeeDashboard/Profile.jsx`)**: rebuilt to the Flutter Profile design — gradient header (no avatar) with name, designation · department, Active/Inactive chip and ID pill; Personal/Employment sections as colored icon-chip rows.
- **Leaves (`components/leave/List.jsx`)**: mobile cards now match the Flutter Leaves card (calendar icon chip, bold type, colored status pill, date range, reason); "Add New Leave" is a bolder button on mobile.
- **Notifications (`pages/NotificationsPage.jsx`)**: flat divided list → individual cards with an icon chip; unread ones get a blue-tinted gradient, bold title and a dot.
- Earlier in this session: Announcements + Tasks (web) and Announcements (Flutter) — see the entry below. `npx vite build` clean after each change.
- **Still NOT redesigned on web (be honest about scope):** Attendance Report, Calendar, Feedback, My Reviews, Settings, Add Leave form, Task Board, Team-lead pages. "Redesign all employee pages" is only partly done.
- Note: a transient `[hmr] Failed to reload TaskList.jsx` console error appeared while that file was mid-edit; the production build is clean — a browser refresh clears it.

## 2026-09-19 06:46 IST — Flutter: fixed check-in location not being captured; Announcements + Tasks card redesigns (web + Flutter)
- **Check-in location was saving "Unknown Area" and the map showed "Map unavailable"** (seen on the Flutter web build). Root causes, all in `mobile/`:
  1. `location_service.dart`: the `geocoding` plugin isn't supported on Flutter web (and can fail on devices w/o Google services), so reverse geocoding always fell back to "Unknown Area". Added the same **OpenStreetMap Nominatim** fallback the web app already uses (`frontend/src/utils/geocodeUtils.js`): native geocoder first (non-web), then Nominatim. Uses a *plain* `Dio`, not `ApiClient.instance.dio`, so the user's auth token is never sent to a third party.
  2. `attendance_screen.dart`: the map was an `Image.network` of a third-party static-map URL (unreliable + CORS-blocked on web). Replaced with `flutter_map` + OSM tiles (`flutter_map ^7.0.2`, `latlong2 ^0.9.1` added to `pubspec.yaml`) — same OSM source as the web embed, works on Android/iOS/web.
  3. `attendance_screen.dart`: `_checkIn`/`_checkOut` submitted whatever `_location` was cached earlier (possibly `null`/"Unknown Area") without refreshing. Added `_freshLocation()`: captures a new fix immediately before submitting (falls back to the last fix); check-in is now blocked with a clear message if no location can be obtained at all.
  - `flutter analyze` clean, `flutter test` 17/17.
- **Announcements list redesigned on both surfaces** (better card visibility): green gradient tile with stronger border/shadow, rounded-square icon chip, 2-line title, 3-line description, NEW pill (<24h), date footer + "Read more ›". Flutter: `announcements_screen.dart` (new `_AnnouncementCard`); web: `components/employee/EmployeeAnnouncements.jsx`.
- **Web Tasks mobile cards now match the Flutter Tasks card** (the earlier gap — web was still the plain title/status/dates card): status icon chip + colored status pill, colored priority pill (High red / Medium orange / Low green), 2-line description, Start/Due row. `frontend/src/components/task/TaskList.jsx`. `npx vite build` clean.


## 2026-09-19 06:40 IST — Web + Flutter: redesigned the Recent Announcements card (and made web→Flutter parity automatic)
- **Standing instruction from the user (recorded in `MEMORY.md` §0 and auto-memory): every frontend mobile-responsive change must also be applied to the Flutter app in the same task, without asking.**
- **Recent Announcements card redesigned on both surfaces, identically:** soft green gradient tile (`#F0FDF4 → white`, `#DCFCE7` border) instead of the plain white card; a rounded-square icon chip (was a circle); title up to 2 lines + description up to 2 lines (was single-line ellipsis); a green **NEW** pill when the latest announcement is under 24h old; and a divider + footer row with a clock icon + relative time on the left and a green "View all ›" on the right (replacing the lone grey chevron).
  - Web: `frontend/src/components/EmployeeDashboard/Summary.jsx` (uses Tailwind `line-clamp-2` — Tailwind 3.4.9 has it built in). `npx vite build` clean.
  - Flutter: `mobile/lib/screens/employee/employee_home_screen.dart` `_buildRecentAnnouncementCard()`. `flutter analyze` clean (0 issues), `flutter test` 17/17 pass.
- **Parity audit of the earlier frontend-mobile work from this session** (per the same instruction): the Home greeting header, colorful Quick Actions, Today's Attendance card, policy-guidance colors, marquee title, bottom nav (employee + admin), closed-by-default sidebar/drawer, card-style salary/template lists — all were *copied from* the Flutter app to the web, so Flutter already had them; nothing further to port. The announcement card above is the first case of a new web design flowing back into Flutter. Web-only by nature (no Flutter equivalent needed): viewport zoom lock, `overflow-x` guard, 16px input-font floor, payslip modal `sm:` stacking.

## 2026-09-19 06:36 IST — Web: fixed header gap + added the mobile app's title marquee animation
- **Gap on all sides of the header**: root cause was double padding — `EmployeeDashboard.jsx`'s wrapper has `p-4 md:p-6` AND `Summary.jsx`'s own root also had `p-4 md:p-6`, so the header's `-mx-4 -mt-4` (escaping padding) only cancelled half of the total 32px inset, leaving a visible 16px gap on left/right/top. Fixed by removing Summary's own redundant mobile padding (`p-4 md:p-6` → `md:p-6`) — the parent's 16px gutter now applies once, which also matches the Flutter app's own 16px page padding (`context.w(16)`). Also dropped `shadow-panel` on mobile (kept on desktop via `md:shadow-panel`) since a drop shadow around a should-be-edge-to-edge banner also reads as a gap. Note: other employee pages (e.g. `AttendanceReport.jsx`) share this same double-padding convention — deliberately not touched here (out of scope, only Home needed a true full-bleed header), but worth knowing if the same "gap" complaint comes up on another page.
- **Title marquee (`SPESHWAY HRMS` glides right→left)**: read `mobile/lib/widgets/marquee_app_bar_title.dart` — a single "SPESHWAY HRMS" (white "SPESHWAY " + emerald `#10B981` "HRMS", Inter 800/15sp/1.4 letter-spacing) continuously glides from the right edge fully off the left edge over 10 seconds, on a repeating loop, clipped to the title area. Replicated in CSS (`.marquee-title` in `index.css`, `padding-left: 100%` + `translateX(0 → -100%)` over 10s linear infinite — same total travel distance as the Flutter version: container width + text width; `prefers-reduced-motion: reduce` disables it and falls back to static text). Applied to both the Home page's own header (`Summary.jsx`) and the generic mobile bar (`Navbar.jsx`) used on every other employee page.
- Verified with `npx vite build` — clean.

## 2026-09-19 06:33 IST — Web: merged the header into one continuous photo-background panel, matching the real mobile app screenshot exactly
- User shared an actual screenshot of the mobile app's home screen: the hamburger/title/bell row and the greeting content are **one single continuous panel** with a photo background (mountains/sunset) and rounded bottom corners — not two stacked elements like the 06:17/06:26 IST passes had (a separate compact dark bar + a separate flat-gradient greeting card below it).
- Copied `mobile/assets/appbar_bg.png` → `frontend/public/images/appbar_bg.png` so both apps use the exact same photo.
- Restructured specifically for the Home page (`Summary.jsx`) to merge them:
  - `EmployeeDashboard.jsx`: detects `isHome` via `useLocation()`; passes `hideMobileBar` to `Navbar` (so its generic mobile bar doesn't render a duplicate on Home) and passes `toggleSidebar` down through `<Outlet context={{ toggleSidebar }}>` so Home's own header can still open the sidebar drawer.
  - `Navbar.jsx`: added the `hideMobileBar` prop (desktop bar unaffected either way); also fixed the title text to "SPESHWAY" (white) + "HRMS" (accent green), matching the screenshot exactly (previously said "SPESHWAY SOLUTIONS").
  - `Summary.jsx`: the greeting block is now one panel with `background-image` (gradient scrim + `appbar_bg.png`), full-bleed on mobile via negative margins (`-mx-4 -mt-4`) to escape the page's own padding, containing the hamburger+title+bell row (mobile-only, via `useOutletContext()` for the sidebar toggle) directly above the greeting/date/quote-pill content — all one rounded-bottom panel, matching the screenshot.
  - Every other employee page (Attendance, Leaves, etc.) is untouched — they still get the plain compact `Navbar` bar as before; only Home builds its own full header.
- Verified with `npx vite build` (clean) and confirmed `appbar_bg.png` is present in `dist/images/`.

## 2026-09-19 06:26 IST — Web: fixed the greeting header still not matching mobile after the first pass
- User pointed out the "Welcome Section" (plain grey avatar circle + "Good Morning, charan" + subtitle, on a flat `bg-brand-800` box) was untouched by the previous redesign and still didn't match. Correct: the mobile app has **no separate greeting card at all** — that content is the app bar's own bottom portion (`_buildHeaderGreeting` in `employee_home_screen.dart`).
- Rebuilt it to match exactly: no avatar circle; small "Good Morning," line; bold name + 👋 emoji; designation · department subtitle (from `dashboardData.employee`, no backend change needed); the date top-right; and a frosted black quote pill with a 🌿 icon wrapping the motivational message — on a `brand-900→brand-800` gradient instead of the old flat `brand-800`.
- Verified with `npx vite build` — clean.

## 2026-09-19 06:24 IST — Web: rebuilt employee dashboard home (`Summary.jsx`) to match the mobile app's actual current design
- Read the real, current `mobile/lib/screens/employee/employee_home_screen.dart` (not memory) per the user's explicit "@mobile/ check the employee dashboard app bar and all features" — it had two whole sections the web version was missing entirely, plus a Quick Actions style mismatch:
  - **Quick Actions**: web previously used 4 solid-color square buttons; rebuilt to match the mobile app's colorful gradient tiles (soft tint background, icon-chip + white circular chevron header row, bold title + muted subtitle), same 2-column-up grid.
  - **Today's Attendance card — added, didn't exist on web before.** Check In / Check Out boxes with colored icon chips, an SVG circular "hours worked" gauge (0–8h progress, teal), a status pill using the exact same status vocabulary/colors as mobile (`Present`/`Present + Overtime`/`Half Day`/`Checked In`/`On Leave`/`WFH`/`Absent`), and a "Go to Live Attendance Tracker" CTA button — all sourced from `dashboardData.todayAttendance` (the existing `/api/dashboard/employee-stats` call already returns `status`/`workingHours`/`inTime`/`outTime`, no backend change needed).
  - **Recent Announcements card — added, didn't exist on web before.** Fetches via the existing `fetchAnnouncements()` helper (`utils/AnnouncementHelper.jsx`, already used elsewhere) in parallel with dashboard stats, shows the latest one with a relative timestamp, links to the announcements page.
  - **Policy Guidance colors realigned** to the mobile app's exact 5-color scheme (green/blue/orange/purple/navy) — web previously reused amber for both "Partial Attendance" and "Break Time Recording", which the mobile app had already fixed to distinct colors in an earlier session.
- Verified with `npx vite build` — clean. No backend changes needed; both data sources (`employee-stats`, `/api/announcement`) already existed and were already used elsewhere on web.
- This only affects the Employee dashboard home page; Admin/HR summaries untouched.

## 2026-09-19 06:17 IST — Web: employee mobile top bar now matches the mobile app's dark app bar
- `Navbar.jsx` (shared by Employee/Admin/HR dashboards) gained a `variant="employee"` mode: on mobile (`md:hidden`) it renders a dark `brand-900→brand-800` gradient bar (hamburger, "SPESHWAY SOLUTIONS", notification bell in white) matching the Flutter mobile app's employee home-screen app bar, while desktop (`hidden md:flex`) keeps the original plain white bar unchanged. Admin/HR dashboards don't pass `variant`, so they're completely unaffected — only `EmployeeDashboard.jsx` opts in.
- The notification bell icon color override uses a **direct-child** selector (`[&>div>button]:!text-white/90`), not a blanket `[&_button]`, specifically because `NotificationBell.jsx`'s dropdown panel (Mark all read / Clear / per-item buttons) is also nested inside the same wrapper — a blanket selector would have turned those white-on-white and made them invisible when opened. Caught and fixed before shipping.
- Verified with `npx vite build` — clean.

## 2026-09-19 06:14 IST — Web: fixed a crash from an earlier fix, redesigned Salary View mobile cards
- **Fixed a real crash**: `frontend/src/components/EmployeeDashboard/MobileBottomNav.jsx` line 19 still referenced `FaBeachAccess` (undefined — the correct `react-icons/fa` export is `FaUmbrellaBeach`) even though the *import* line had already been corrected earlier in this session — the usage site was missed, breaking the entire employee dashboard with a `ReferenceError` on every page load. Fixed the usage to match the import; confirmed with a repo-wide grep that zero references to the wrong name remain anywhere.
- **Redesigned `View.jsx`'s "Salary Records" list into mobile cards** (`employee-dashboard/salary/:id` — the table's "Actio..." column was cut off on mobile). Added a `sm:hidden` card list alongside the existing desktop table (`hidden sm:block`), then iterated per feedback: started as a 2-column `aspect-square` grid → user asked for one card per row → `aspect-square` on a full-width card produced a huge, mostly-empty tile → dropped `aspect-square` entirely for a compact natural-height card (index + date header, prominent Net Salary, then Emp ID/Salary/Deduction in a tight 3-column row below a divider).
- Verified with `npx vite build` after each iteration — all clean.

## 2026-09-19 06:07 IST — Web: redesigned Payroll Template Manager for mobile, added admin bottom nav
- **`PayrollTemplateManager.jsx`**: the `react-data-table-component` list didn't collapse to cards on mobile (only hid 2 columns via `omit: isMobile`), causing the cramped/truncated "Actio..." column seen in a screenshot. Added a proper `sm:hidden` mobile card list (same pattern as `TaskList.jsx`/`TeamList.jsx`) showing template name + Default/Active badges, employee, Basic/Net Salary, and Edit/Set Default/Delete actions per card; desktop keeps the existing `DataTable` (`hidden sm:block`). Also made the "Existing Templates" header/search row stack properly on mobile (`items-stretch` instead of forced `items-center`, full-width search).
- **Added a mobile bottom nav for the admin dashboard** (`frontend/src/components/dashboard/AdminMobileBottomNav.jsx`, wired into `AdminDashboard.jsx`, `md:hidden`): 4 tabs — Leaves / Home (center) / Employees / Settings — mirroring the Flutter mobile app's admin tab layout, per the user referencing "how it is in mobile" for admin/dashboard navigation (same request pattern as the earlier employee `MobileBottomNav.jsx`). Sidebar/hamburger unchanged for everything else. Added matching `pb-24 md:pb-6` bottom padding to the content area.
- Verified with `npx vite build` — clean.
- No mobile (Flutter) parity needed — this mirrors the Flutter app's existing navigation *into* the web app, not the other direction.

## 2026-09-19 06:03 IST — Web: locked viewport zoom, fixed sidebar-open bug, added mobile bottom nav, fixed payslip modal cramping
- **Viewport zoom locked outright**: user repeated the "no zoom in/out, should fit screen" request several times, including a screenshot showing genuinely huge/clipped text consistent with the page being pinch-zoomed. Updated `frontend/index.html`'s viewport meta to `width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover` — this fully disables pinch-zoom, on top of the 05:48 IST CSS-level fixes (16px input floor, global overflow-x guard). Verified the flagged "huge text" wasn't a real code bug — `AdminSummary.jsx`'s `StatCard` numbers are genuinely `text-xl md:text-2xl` (~20-24px); the huge/clipped appearance only makes sense as an already-zoomed-in viewport, which the new meta tag now prevents from ever happening.
- **Fixed sidebar defaulting open on mobile**: `useState(true)` in `EmployeeDashboard.jsx`/`AdminDashboard.jsx`/`HRDashboard.jsx` hardcoded the sidebar open on every load regardless of screen size, so it covered the whole screen on mobile by default. Fixed to initialize from `window.innerWidth >= 768` (matches the existing `md:ml-64` breakpoint) in all three.
- **Added a mobile bottom nav for employees** (`frontend/src/components/EmployeeDashboard/MobileBottomNav.jsx`, wired into `EmployeeDashboard.jsx`, `md:hidden`): 5 tabs — Leaves / Attendance / Home (center, elevated) / Tasks / Profile — mirroring the Flutter mobile app's bottom tab bar, per explicit request ("for employee i want same like in mobile home screen and bottom"). The existing hamburger + sidebar drawer stay as-is for everything else (Salary, Announcements, Team Lead sections, etc.), matching how the mobile app pairs a bottom bar with a side drawer for "more" links. Added `pb-24 md:pb-6` to the content area so it doesn't sit under the new bar.
- **Fixed severe mobile cramping in the payslip preview/history modals** — found via a screenshot showing unreadably tiny text: both `PayslipPreview.jsx` (the "Generate Payslip" preview) and `View.jsx` (payslip history detail) used `grid-cols-2` / fixed 4-column (`width: '25%'`) layouts with no responsive breakpoints, squeezing a whole two/four-column printable-document layout into a ~380px-wide screen. Fixed both to stack to 1 column (`View.jsx`: 2 cells per row via `w-1/2` so label+value pairs stack) below the `sm` breakpoint, while keeping the exact desktop/print appearance unchanged. Also converted several fixed pixel `height`s to `minHeight` in `View.jsx` so wrapped text doesn't get clipped, and made the company-name header stack the logo above the text on mobile instead of using absolute positioning that could overlap.
- Verified with two full `npx vite build` runs (before and after the payslip modal fixes) — both clean.
- No mobile (Flutter) parity needed for any of the above — these are web-only viewport/layout/navigation fixes; the Flutter app already has its own bottom nav and responsive system.

## 2026-09-19 05:48 IST — Web: global mobile-responsive/zoom fixes
- **Root-caused the "page zooms in/out" complaint**: it's not a viewport-meta issue (that was already correct) — it's iOS Safari's well-known behavior of auto-zooming the whole page when a focused form field's computed font-size is under 16px. Fixed by flooring `input`/`select`/`textarea` at `font-size: 16px` on screens `<768px` in `frontend/src/index.css` (desktop keeps whatever each component already sets).
- Added a global overflow guard (`html, body, #root { max-width: 100%; overflow-x: hidden; }`) so a single element anywhere in the app with an accidental fixed/min width can no longer widen the whole document and force horizontal pinch-zoom/pan to see cut-off content — a blanket safety net regardless of which specific component is at fault on a given page.
- Added `-webkit-text-size-adjust: 100%` to stop mobile browsers auto-inflating text size on orientation change (a related "unexpected zoom" trigger).
- **Audited the app for structural responsive issues** (not just this CSS fix): checked all 3 dashboard shells (`AdminDashboard.jsx`/`HRDashboard.jsx`/`EmployeeDashboard.jsx`) — both use `max-w-[1800px] mx-auto w-full`, a safe *cap*, not a fixed min-width; checked all 15 files with raw `<table>` elements — every one already wraps its table in `overflow-x-auto` (some additionally provide a `sm:hidden` mobile card-list alternative, e.g. `TaskList.jsx`/`TeamList.jsx`/`TeamDetail.jsx`/`AdminAttendanceReport.jsx`/`leave/List.jsx`); checked for hardcoded 3+ column grids missing responsive breakpoints — the only two hits (`Calendar.jsx` grid-cols-7, `MonthPicker.jsx` grid-cols-3) are inherently fixed-column widgets (a 7-day week, a 3×4 month grid) bounded by their own container, not page-level layout bugs.
- **Conclusion**: the codebase already follows solid Tailwind responsive conventions in the places checked; the CSS-level fixes above address the actual reported symptom (unwanted zoom) at its root, application-wide, in one place, rather than needing a per-page patch. A true pixel-by-pixel visual audit of every remaining page would need live browser testing at multiple breakpoints (or specific screenshots of a broken page, as the user has provided successfully for other fixes in this project) — flagged as follow-up if a specific page is still found broken after this fix.
- No mobile (Flutter) parity needed — this is a web-viewport/CSS-specific fix; the Flutter app has its own separate `context.w/h/sp` responsive system already in place.

## 2026-09-18 12:36 IST — Web: "Generate Payslip by Days" — final Net Pay formula (supersedes the 12:06 IST entry below)
- User iterated twice more after the 12:06 IST change, converging on the actual target: Earnings show **full** template values, **LOP Days = 0**, **LOP Amount = 0**, **Other Deductions = 0** (Total Deductions = PF + Prof Tax only) — matching the reference payslip's displayed figures exactly — while Net Pay is still genuinely computed from days actually worked.
- Worked out the real formula behind the reference PDF (`PRABHUSWAMY M_AUG.pdf`): `Net Pay = (full gross earnings × daysWorked ÷ calendarDaysInMonth) − (PF + Prof Tax)`. Verified this reproduces the reference's exact Net Pay (22006.45) for its numbers. This is **not** achievable via `Total Earnings − Total Deductions` alone (that formula, used everywhere in this codebase, would force Net Pay to 36200 given Earnings=40000/Deductions=3800) — it requires an explicit override point.
- Added `netPayOverride` end-to-end:
  - `GeneratePayslipByDays.jsx` `buildPayload()`: computes it as `max(0, proratedGrossEarnings − (pf + proftax))` and includes it in the payload; Basic/DA/HRA/etc., LOP Days/Amount, and Other Deductions are all sent unchanged (full/zero) as before.
  - `server/controllers/payslipController.js` (`generatePayslip` and `previewPayslip`): now use `payload.netPayOverride` for the stored/returned `netSalary` when present, otherwise fall back to the original `totalEarnings − totalDeductions` — **zero behavior change** for `PayslipGenerator.jsx`'s normal flow, which never sends this field.
  - `server/utils/pdfGenerator.js`: PDF's Net Pay line now prefers `salary.netSalary` (the authoritative stored value) over recomputing from the earnings/deductions it just rendered.
  - `frontend/src/components/salary/PayslipPreview.jsx`: same — prefers `payslip.netSalary` over its local recompute.
  - `frontend/src/components/salary/View.jsx` (payslip history detail view): found and fixed the **same** unsafe recompute independently — would have silently reintroduced the wrong Net Pay when viewing a saved by-days record from history.
  - Verified via a standalone Node calc that the new formula reproduces the reference PDF's Net Pay (22006.45) exactly for its own numbers (40000 gross, 20/31 days worked, 3800 fixed deductions).
- Confirmed no other Net Pay recompute sites needed changes: `PayslipGenerator.jsx`, `PayrollTemplateManager.jsx`, and `payrollTemplateController.js` all compute Net Pay for unrelated flows that never receive a `netPayOverride`.
- Mobile already correct — `payslips_screen.dart` reads `netSalary` directly from the API response rather than recomputing, so it displays the right value with no changes needed.

## 2026-09-18 12:06 IST — Web: "Generate Payslip by Days" now shows a real, visible LOP line (reversed the earlier hidden-proration approach) — SUPERSEDED, see entry above
- User compared a preview against the reference `PRABHUSWAMY M_AUG.pdf` again. Confirmed that PDF still has a genuine, pre-existing arithmetic bug (Total Earnings 40000 − Total Deductions 3800 = 36200, but it prints Net Pay as 22006.45 — self-inconsistent, a stale artifact, not a template to match) — not the cause of the reported "difference."
- The real issue: `GeneratePayslipByDays.jsx` prorated every earnings line item (Basic/DA/HRA/etc.) by `daysWorked / calendarDaysInMonth`, while always showing `LOP Days: 0` — technically self-consistent (Total Earnings − Total Deductions = Net Pay held), but confusing since a real pay cut was invisible as "0 LOP".
- **User's explicit direction**: earnings should show full template values (never scaled down); Loss of Pay should be a real, visible, non-zero deduction line; and Net Pay should still be reduced internally via that deduction. This **reverses** the earlier "hide LOP, cut via earnings proration" decision from the previous session — noted in `MEMORY.md` accordingly.
- Implemented in `buildPayload()`: Basic/DA/HRA/Conveyance/Medical/Special allowances now always pass their full, unprorated template values. LOP Days = `totalDays − daysWorked` (calendar days in month), LOP Amount = `(full gross earnings ÷ calendar days in month) × LOP Days`, sent with `autoCalculateLOP: false` so the backend (`payslipController.js`, both `previewPayslip` and `generatePayslip`) uses that precomputed amount as-is rather than recalculating it. Updated the on-page hint text and header description to match the new behavior.
- Backend required no changes — `previewPayslip`/`generatePayslip` already honored explicit `lopamount`/`lopDays` when `autoCalculateLOP` is false (same pattern already used for PF).
- No mobile parity needed — this admin-only "Generate Payslip by Days" screen has no mobile equivalent (mobile's payslip screens are read-only employee views).

## 2026-09-18 07:10 IST — Mobile: diagnosed physical-device login failure + wired network screen into Login
- **Root cause of "Could not reach the server" on login**: user was running the app on a **physical Android phone**, not the emulator. `Env.developmentBaseUrl` defaults to `http://10.0.2.2:5001` for Android debug builds, which is an alias that only resolves on the Android **emulator** — a real device can't reach it. Fix is to run with `--dart-define=API_BASE_URL=http://<PC-LAN-IP>:5001` (this machine's current LAN IP: `192.168.1.17`), with phone and PC on the same Wi-Fi. Not a code bug — `env.dart` already documents this limitation in its header comment; this was a "how to run for physical-device testing" gap, not a defect.
- **Fixed a real gap**: login failures only ever showed a plain dark `SnackBar`, never the illustrated `NetworkErrorView` from `state_views.dart`, even for genuine connectivity failures. Fixed:
  - `AuthProvider` (`auth_provider.dart`) now keeps the raw caught exception in `lastErrorRaw` (previously only the stringified `lastError` survived), needed so `isNetworkError()` can inspect it.
  - `login_screen.dart`: on a failed login, checks `isNetworkError(auth.lastErrorRaw)` — if true, shows the illustrated `NetworkErrorView` (with Retry) above the login form instead of a snackbar; the form itself stays mounted via `Offstage` (not removed from the tree) so `_formKey`/typed email+password survive and Retry can resubmit without a null-check crash. Non-network failures (wrong password, etc.) still use the snackbar since the user needs the form visible to fix their input.
- `flutter analyze` clean, all 17 tests pass.
- No web parity needed — `frontend`'s Login.jsx has its own inline error text pattern; this is a mobile-specific illustrated-state wiring change.

## 2026-09-18 07:04 IST — Mobile: illustrated state screens (network/error/empty/success)
- Rebuilt `mobile/lib/widgets/state_views.dart` from bare icon+text widgets into fuller "illustrated" full-page states: a layered soft-circle badge behind the icon (concentric rings) with a scale+fade entrance animation (skips animation under reduced-motion). `NetworkErrorView`, `ErrorView`, `EmptyStateView` kept the same public API (no call-site changes needed) — just look more like real screens now.
- Added a new **`SuccessView`** (didn't exist before) — full-page success confirmation state (green check badge, title/subtitle/action).
- Wired `SuccessView` into a real flow: the "Request Account Deletion" bottom sheet in `profile_screen.dart` now transitions to a `SuccessView` (with a "Done" button) instead of popping immediately + a snackbar, giving a clear confirmation moment.
- `flutter analyze` clean, all 17 existing tests still pass.
- No web parity needed — these are mobile-native full-screen state components; web already has its own `LoadingState`/`EmptyState` components (`frontend/src/components/common/`) serving the same purpose in that codebase's idiom.

## 2026-09-18 06:43 IST — Mobile: splash screen spinner removed
- Removed the `CircularProgressIndicator` from `mobile/lib/screens/splash_screen.dart` per explicit request — splash now shows only the fade+scale logo and "SPESHWAY HRMS" wordmark, no loading spinner. No web equivalent (web has no splash screen), so no parity change needed.

## 2026-09-18 IST — Mobile: login screen keyboard overflow fix
- Fixed a real "BOTTOM OVERFLOWED BY 95 PIXELS" bug on `mobile/lib/screens/login_screen.dart`: when the keyboard opens while the Email field is focused, the top `Expanded` brand-header region (logo + "SPESHWAY HRMS" + tagline) shrinks below its natural content height and overflows.
- Fix: wrapped `_buildBrandHeader()` in a fixed-width (`280`) `SizedBox` + `FittedBox(fit: BoxFit.scaleDown)`, so the header scales down as a unit instead of overflowing when vertical space is tight (keyboard open, small phones, etc.), while still wrapping the tagline text at a sane width before scaling.
- Web-only parity note: not mirrored to `frontend/src/pages/Login.jsx` — that page doesn't use this shrink-on-keyboard layout (already a centered single-block design from an earlier redesign), so there's no equivalent bug there.

## 2026-09-18 05:09 IST — Mobile: user seed credential update
- Updated `server/verificationSeed.js` password from `verification123` to `verify@123` and re-ran the seed (upserts by email, no duplicate created).
- Verification/App-review login is now: `verification@gmail.com` / `verify@123`.

## 2026-09-17 (evening) IST — Play Store production readiness pass
- **Release signing**: confirmed `android/key.properties` + upload keystore now wired into `android/app/build.gradle.kts` release `signingConfig` (previously fell back to debug signing — this was the P0 blocker). Verified with a real `flutter build appbundle --release` → succeeded, produced a properly release-signed AAB.
- **Fixed a real syntax bug** in `mobile/lib/screens/profile_screen.dart` (`_showDeletionRequestSheet`): three closing tokens were swapped (`);`/`},`/`),` out of order), which broke `flutter analyze` entirely. Fixed; `flutter analyze` is now clean (0 issues).
- **Fixed a genuine Firebase project mismatch**: `mobile/lib/firebase_options.dart` (what `Firebase.initializeApp()` actually uses) pointed to project `speshway-hrms`, while `android/app/google-services.json` and `server/service-account.json` (the backend's FCM sender) both pointed to `new-hrms-d8eaf` — two different Firebase projects, which would silently break push notification delivery (FCM rejects cross-project token/send mismatches). Fixed both the Android and iOS blocks in `firebase_options.dart`, and replaced `mobile/ios/Runner/GoogleService-Info.plist` with a real config generated under `new-hrms-d8eaf`. All four config sources (Android native, iOS native, Dart Android, Dart iOS) now agree.
- **Added a test suite** (previously `test/` didn't exist at all, so `flutter test` errored with "Test directory not found"): added `mobile/test/models/user_test.dart`, `mobile/test/services/api_client_test.dart`, `mobile/test/widgets/status_pill_test.dart` — 17 tests, all passing.
- **Verified account-deletion flow already exists end-to-end** (backend `AccountDeletionRequest` model/controller/routes at `/api/account/deletion-request` + `/api/account/deletion-requests`, a public web page `frontend/src/pages/DeleteAccount.jsx`, and a mobile "Request Account Deletion" card + bottom sheet in `profile_screen.dart`) — this is the correct pattern for an org-provisioned (admin-creates-credentials) app: employees request deletion, HR/Admin verifies and actions it, rather than instant self-deletion.
- Deliberately **not** bumped: Gradle 8.14→9.1+ / AGP 8.11.1→9.0.1+ / Kotlin 2.2.21→2.3.20+ — these are deprecation warnings, not current failures; user chose to defer this to a separate session given the risk of breaking the now-verified release build.
- Added `server/verificationSeed.js` — seeds a stable `verification@gmail.com` login (role: employee, fully populated Employee record) for Play Store "App access" reviewer credentials / general store verification. Safe to re-run (upserts by email).

## 2026-09-17 (afternoon) IST — Mobile Documents removal, splash screen, home header contrast, profile redesign, drawer icons
- Removed the "Documents" feature entirely from mobile (employee `documents_screen.dart`, admin `admin_documents_screen.dart`, `document_service.dart`, and their drawer entries) per explicit request.
- Redesigned `mobile/lib/screens/splash_screen.dart`: fade+scale entrance animation, a spinner, an `errorBuilder` fallback if the logo asset fails to decode, and a 500ms cross-fade transition into Login/Home instead of an instant cut.
- Fixed poor text contrast on the Employee Home screen's greeting header (background photo was too bright behind white text) — strengthened the gradient scrim (3-stop, darker toward the text) and added text shadows to every line in `_buildHeaderGreeting()`.
- **Fixed a real backend bug**: `POST /api/auth/login` never returned `email` in the `user` object (only `_id, name, role`) — this is why the mobile Profile screen's Email field was blank. Fixed in `server/controllers/authController.js`. Also added a mobile-side fallback (`emp.userId.email`) for already-cached sessions predating the fix.
- Redesigned `profile_screen.dart`: removed the avatar circle per request, moved the Active/Inactive status into the header card (removed the now-duplicate block from Employment Information), trimmed fields to match web's `employee-dashboard/profile` page exactly (dropped Marital Status/Work Location/Shift/Bank & Tax — those aren't shown on web).
- Overhauled `mobile/lib/widgets/app_drawer.dart`: every item now has a colored icon chip (thematically grouped — blue/green/purple/orange/teal/indigo/pink) instead of plain monochrome icons, across employee sections, admin sections, and the footer (Profile/Settings/Logout).

## 2026-09-17 (midday) IST — Mobile bug fixes + broad screen redesign
- Attendance screen: removed "Recent History" section, added a real static-map image (OpenStreetMap free static-map service, no new package) to the Location card, colorized the Check-In/Check-Out icon chips and Today's Summary rows to match web's colorful style.
- Leaves screen: removed the "Leave Balance" section; fixed the Submit Request UX (was giving one generic "fill in all fields" error) — now shows field-specific inline errors on the date pickers.
- Attendance Report screen: removed the "Request Regularization" bottom sheet/FAB entirely (deleted `_RegularizationSheet` class and related state); fixed cramped 3-column In/Out/Worked row alignment.
- Calendar screen: replaced the "All Events & Holidays" full list with a real month-grid calendar (custom-built, no new dependency) that defaults to today and filters events by tapped day.
- App-wide states: added `mobile/lib/widgets/skeleton_loader.dart` (shimmer skeletons) and `mobile/lib/widgets/state_views.dart` (`NetworkErrorView`/`ErrorView`/`EmptyStateView` + `buildErrorState()` helper that branches on a new `isNetworkError()` in `api_client.dart`).
- Added `mobile/lib/services/data_cache.dart` — a generic `DataCache<T>` + `DataCaches` registry (attendanceToday, attendanceMonthly, leavesList, tasksList, payslipHistory, employeeProfile, events), wired into `main.dart` via `MultiProvider`. Rolled stale-while-revalidate caching into Attendance, Attendance Report, Leaves, Tasks, Payslips, Profile, and Calendar screens so revisiting them from Home doesn't re-show a full loading spinner every time.

## 2026-09-17 (earlier) IST — Attendance/payroll/mobile parity work
## 2026-09-23 IST — Mobile: notification audit & release APK build
- Performed end-to-end audit of notification architecture across Flutter mobile app (`PushService`, `NotificationService`, `AppEvents.unreadNotifications`, `NotificationsScreen`) and Node backend (`fcmService.js`, `notificationController.js`, models, and controllers). Verified that all Firebase project references match `new-hrms-d8eaf` (project `800902168034`) across `google-services.json`, `firebase_options.dart`, `GoogleService-Info.plist`, and `service-account.json`.
- Fixed a minor flow control lint in `mobile/lib/screens/login_screen.dart` (enclosed if block in braces for `curly_braces_in_flow_control_structures`).
- Ran `flutter test` (17 unit tests passed).
- Built release-signed APK (`mobile/build/app/outputs/flutter-apk/app-release.apk`, 60.4 MB) for local testing.
- Bumped app version to `1.0.2+3` in `mobile/pubspec.yaml` (versionCode 3, versionName 1.0.2) to satisfy Google Play requirement after versionCode 2 had already been used.
- Built release-signed Android App Bundle via `flutter build appbundle --release` -> generated `mobile/build/app/outputs/bundle/release/app-release.aab` (59.0 MB) ready for upload to Google Play Console.
